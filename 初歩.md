# ① NW運用保守｜案件参画前チートシート 🧠⚡

**目的：これだけ見直して、現場の会話で固まらない。**
Cisco中心・L2/L3/FW・Tera Term・障害切り分け向け。添付資料＋2026年のCisco IOS XE 26.x公式で再確認済み。 ([Cisco SPG][1])

---

## 🗺️ まず「レイヤー」の地図

| 層      | 見るもの               | 一言              |
| ------ | ------------------ | --------------- |
| 🔌 L1  | Cable / SFP / Link | **物理的につながってる？** |
| 🔀 L2  | MAC / VLAN / STP   | **同じLAN内で届ける**  |
| 🛣️ L3 | IP / Route / GW    | **別NWへの道を選ぶ**   |
| 🚪 L4  | TCP/UDP / Port     | **どのサービスへ届ける？** |
| 🌐 L7  | HTTP/DNS等          | **アプリとして正常？**   |

🔥 **Link Up ≠ 通信正常 ≠ アプリ正常**

---

# 🔌 L1｜物理

**Link Down**
→ Cable・SFP・NIC・相手電源・Portなどを確認。

**administratively down**
→ ケーブルではなく、`shutdown` 等で**設定上Port停止**。

**CRC / FCS Error**
→ Frame破損の手掛かり。継続増加ならCable・SFP・NIC等を疑う。

**CRC＝検算方法 / FCS＝検算結果をFrame末尾に入れる場所。**

---

# 🔀 L2｜MAC・VLAN

**Switchは送信元MACを学習 → 宛先MACを見て転送。**

```text
送信元MAC → 覚える 🧠
宛先MAC   → 転送先を探す 🔎
```

**ARP Table**
→ `IP ↔ MAC`

**MAC Address Table**
→ `MAC + VLAN ↔ Switch Port`

🔥 IPから端末を追うなら、

```text
IP
 ↓ ARP
MAC
 ↓ MAC Table
Switch Port
```

---

# 🏘️ VLAN / Subnet

**VLAN＝L2を論理的に分割**
**Subnet＝L3/IPの範囲を分割**

基本設計は、

```text
VLAN10 → 192.168.10.0/24
VLAN20 → 192.168.20.0/24
```

のように **1 VLAN ≒ 1 Subnet** が多い。
⚠️ でも「VLAN＝Subnet」ではない。

---

# 🚪 Access / Trunk

**Access Port**
→ PC・Printerなどを基本1 VLANへ収容。

**Trunk Port**
→ 1本のLinkで複数VLANを802.1Q Tag付きで運ぶ。

🔥 **TrunkはVLANを運ぶだけ。VLAN間Routingはしない。**

```text
VLAN10 ─┐
VLAN20 ─┼→ Trunk → L3SW
VLAN30 ─┘
```

---

# 🔁 STP

**STP＝L2 Loop防止。**

冗長Linkの一部をBlocking/Discardingにして、Broadcast Stormを防ぐ。

⚠️ **Blocking＝障害とは限らない。**
勝手にForwardingへ戻すのは危険。

---

# 🛣️ L3｜IP・Gateway・Route

同一Subnetなら👇

```text
PC → ARPで相手MAC取得 → 直接送る
```

別Subnetなら👇

```text
PC → ARPでGWのMAC取得
   → Default Gateway
   → Routing
```

🔥 **「同じVLAN」よりまず「同じSubnetか？」で考える。**

---

# 🚪 SVI

**SVI＝VLANをL3 Routingへつなぐ仮想Interface。**

```text
VLAN10
192.168.10.0/24
      ↓
SVI 192.168.10.1
      ↑
端末のDefault Gateway
```

L3SWではSVIが各VLANのGWになる構成が多い。Cisco公式でもSVIはVLANをRouting機能へ接続するInterfaceとして定義されてる。([Cisco][2])

⚠️ **SwitchがIPを持つ＝必ずRoutingしている、ではない。**
L2SWも管理用IPを持てる。

---

# 🗺️ Routing

**Route Table＝「この宛先なら次どこへ送る？」一覧。**

```text
Destination
→ Next Hop
→ Outgoing Interface
```

**最長一致**
→ 一致するRouteの中で、より具体的なPrefixを優先。

```text
10.20.0.0/16
0.0.0.0/0

10.20.1.10宛
→ /16を選ぶ
```

🔥 **行きRouteだけで安心しない。戻りRouteも必要。** 

---

# 🚧 通信成立の5点セット

```text
① 行きRoute
② FW / ACL許可
③ ServerがPort Listen
④ 戻りRoute
⑤ 戻り側の通信制御
```

🔥 **道がある ≠ 通してもらえる ≠ 相手が受付してる**

---

# 🔥 Firewall / ACL

最低限見る👇

```text
Source
Destination
Protocol
Port
Action
Direction
```

さらに、

**Rule順序 / Zone / Stateful・Stateless**

も確認。

**Stateful**
→ 許可済みSessionの返事を追跡。

**Stateless**
→ 行き・戻りを個別に考える。

⚠️ Statefulでも**戻りRoute自体は必要**。

---

# 🔄 NAT

**NAT＝IP/Portを書き換える。FWではない。**

**SNAT** → Sourceを書き換える
**DNAT** → Destinationを書き換える
**PAT** → Portも使って1 Public IPを複数端末で共有

🔥 **NATした ≠ 通信許可した**

---

# 🚚 TCP / UDP

**TCP**
→ 接続型。順序・再送あり。

**UDP**
→ TCPのような接続確立・再送保証なし。

TCP接続開始👇

```text
SYN
 ↓
SYN/ACK
 ↓
ACK
```

**RST**
→ Reset・明示的拒否など。

**Timeout**
→ Route / FW Drop / 戻りRoute / Packet Loss等、原因多数。

---

# 🚪 頻出Port

|        Port | 用途    |
| ----------: | ----- |
|      TCP 22 | SSH   |
|  TCP/UDP 53 | DNS   |
|   UDP 67/68 | DHCP  |
|      TCP 80 | HTTP  |
|     UDP 123 | NTP   |
| UDP 161/162 | SNMP  |
|     TCP 443 | HTTPS |
|    TCP 3389 | RDP   |

🔥 **ICMPにはTCP/UDP Port番号はない。**

---

# 🌐 Pingの認識

`ping`成功👇

> **ICMPが往復できた。以上。**

まだ不明👇

```text
DNS ❓
TCP/443 ❓
TLS ❓
HTTP ❓
Application ❓
```

Windowsなら、

```text
ping
→ ICMP

Resolve-DnsName
→ DNS

Test-NetConnection -Port 443
→ TCP Port

curl.exe -v
→ TLS / HTTP
```



---

# 📖 DNS

**DNS＝名前から必要なResource Recordを調べる。**

最低限👇

```text
A     → IPv4
AAAA  → IPv6
CNAME → 別名
PTR   → IPから名前
```

🔥

```text
IP指定 → ○
名前指定 → ×
```

なら**DNSを優先確認**。

DNSは **UDP/53だけじゃない。TCP/53も使う。**

---

# 📮 DHCP

DHCPは、

```text
IP
Subnet Mask
Default Gateway
DNS
```

などを自動配布。

DORA👇

```text
Discover
Offer
Request
Acknowledge
```

**169.254.x.x**
→ Link-local。WindowsならDHCP取得失敗の手掛かりになることが多い。

**DHCP Relay / ip helper**
→ 別SubnetのDHCP Serverへ要求を中継。

---

# 🖥️ Cisco / Tera Term

**Tera Term＝Cisco専用ではない。**
SSH等でCisco・Linuxなどへ接続するTerminal。

新規管理接続は基本👇

```text
SSH v2 ✅
Telnet  ⚠️ Legacy / 平文
```

---

# 🎮 Cisco CLIモード

```text
Switch>
User EXEC

Switch#
Privileged EXEC

Switch(config)#
設定変更 🚨

Switch(config-if)#
Interface設定 🚨
```

🔥 調査だけなら基本 **`#` + show系**。

---

# 👀 Cisco頻出show

```text
show clock
→ 時刻

show version
→ 機種・IOS・Uptime

show interfaces status
→ Port一覧

show interfaces <IF>
→ Port詳細・CRC等

show vlan brief
→ VLAN

show interfaces trunk
→ Trunk / Allowed VLAN

show mac address-table
→ MAC → Port

show ip arp
→ IP → MAC

show ip interface brief
→ SVI / IP / Up-Down

show ip route
→ Route

show running-config
→ 現在Config

show startup-config
→ 保存Config

show logging
→ Log
```

Cisco IOS XE 26.x公式でも `show interfaces` / `show ip interface brief` / Trunk確認などが現行コマンドとして確認できる。([Cisco SPG][1])
添付資料の最低限コマンドも同じ軸。

---

# 💾 Running / Startup Config

```text
running-config
＝ 今動いてる設定 🏃

startup-config
＝ 再起動時に読む保存設定 💾
```

`exit` / `end`
→ **設定Undoではない。**

`copy running-config startup-config`
→ **確認ではなく保存操作。**

⚠️ 勝手に実行しない。

---

# 📡 監視

**SNMP**
→ 状態・数値を取得。

**syslog**
→ 機器で起きた出来事を記録。

**NTP**
→ 時刻同期。

🔥 **ログの時刻がズレると障害調査が地獄。**

---

# 🚨 障害時の最初の5問

```text
いつから？
誰/どこまで影響？
何ができない？
どの方向？
直前変更ある？
```

その後、

```text
L1 → L2 → L3 → 戻りRoute
→ FW → DNS → Port → Application
```

で潰す。

---

# 🛡️ 運用で絶対事故らない5原則

```text
① 勝手に変更しない
② Before状態を残す
③ 1変更ずつ
④ 想定外なら止める
⑤ 事実・推測・未確認を分ける
```

`reload` / `shutdown` / `clear` / 高負荷debug / FW Flush
→ **「試しに」は絶対やらない。** 

---

# ⚡ 入場直前30秒チェック

```text
L2   → MAC / VLAN / Trunk / STP
L3   → IP / GW / Route / SVI
L4   → TCP/UDP / Port
FW   → Src / Dst / Protocol / Port / Direction
ARP  → IP ↔ MAC
MAC表 → MAC ↔ Port
Ping → ICMPだけ
SVI  → VLANのL3出口
NAT  → 変換、FWではない
SSH  → Tera Termから接続
show → 状態確認
変更 → 承認・証跡・Rollback
```

**①はこれを参画前の「毎日1〜2分見る紙」にすれば十分。**
②は許可をもらったら、**「症状 → 最初に見る場所 → コマンド → よくある原因 → 次の一手」**の字引き形式で作る。

[1]: https://spg.xgslb-v3.cisco.com/c/en/us/td/docs/switches/lan/catalyst9300/software/release/26-x/command_reference/b_26x_9300_cr/interface_and_hardware_commands.html?utm_source=chatgpt.com "Command Reference, Cisco IOS XE 26.x.x (Catalyst 9300 Switches) - Interface and Hardware Commands [Cisco Catalyst 9300 Series Switches] - Cisco"
[2]: https://www.cisco.com/c/en/us/td/docs/switches/lan/catalyst9300/software/release/26-x/configuration_guide/int_hw/b_26x_int_and_hw_9300_cg/configuring_interface_characteristics.html?utm_source=chatgpt.com "Interface and Hardware Components Configuration Guide, Cisco IOS XE 26.x.x (Catalyst 9300 Switches) - Configuring Interface Characteristics [Cisco Catalyst 9300 Series Switches] - Cisco"
