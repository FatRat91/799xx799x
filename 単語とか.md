## 結論 🎯

Ansibleは**複数の機器へ設定を一括配布する司令塔**です🤖

```text
Ansibleが設定する
      ↓
VLAN       = ネットワークを部屋分け
NTP        = 時計を合わせる
syslog     = ログを集める
SNMP       = 状態を監視する
OSPF/BGP   = 経路を自動交換する
```

Ansible自体が通信経路を決めるわけではありません。
**OSPF・BGPを設定した後、機器同士が自動で経路交換**します。

---

# 役割の違いを一撃整理 ⚔️

| 設定     | レイヤー | 一言         | 使用場面               |
| ------ | ---- | ---------- | ------------------ |
| VLAN   | L2   | 部屋分け🚪     | 社員・サーバー・ゲストを分離     |
| NTP    | 管理   | 時計合わせ⏰     | 全機器のログ時刻を統一        |
| syslog | 管理   | 日記送信📝     | 障害・設定変更ログを集中保存     |
| SNMP   | 管理   | 健康診断📊     | CPU、帯域、ポート状態を監視    |
| OSPF   | L3   | 社内カーナビ🗺️  | 組織内部の経路を自動交換       |
| BGP    | L3   | 国同士の経路交渉🤝 | ISP・拠点・クラウド等との経路交換 |

---

# Ansibleで何が便利？ 🤖

例えばスイッチ50台へ手作業するとします。

```text
VLAN 30を追加
NTPを10.0.0.10へ統一
syslogを10.0.0.20へ送信
SNMPv3を有効化
```

手作業だと👇

* 😵 入力ミス
* 🐌 時間がかかる
* 🧩 機器ごとに設定がバラバラ
* 📝 誰が何を変えたか分かりにくい

Ansibleなら👇

```text
Gitの設定
   ↓ レビュー
Ansible
   ↓ 管理NW経由
SW01・SW02・SW03…へ一括適用
```

現在のCisco IOS向けAnsible Collectionには、VLAN・NTP・syslog・SNMP・OSPF・BGPを管理する専用Moduleがあります。[Ansible公式](https://docs.ansible.com/projects/ansible/latest/collections/cisco/ios/index.html)

主な接続方法はCLI over SSH・NETCONF・HTTPS APIです。[Ansible公式](https://docs.ansible.com/projects/ansible/latest/network/getting_started/network_differences.html)

---

# OSPFとBGPの使い分け 🗺️

## OSPF＝社内カーナビ

同じ会社・組織内のルータ同士で経路を共有します。

```text
東京拠点 ─ 大阪拠点 ─ 福岡拠点
       ← OSPFで経路交換 →
```

回線が切れたら、別ルートを自動計算します。

```text
通常：東京 → 大阪 → 福岡
障害：東京 → 名古屋 → 福岡
```

特徴：

* 🏢 組織内部向け
* ⚡ 障害時の経路切替が比較的速い
* 📏 主にコストが低い経路を選ぶ
* 🧩 Areaでネットワークを分割可能

Cisco公式でもOSPFは組織内部向けのIGPとして定義されています。[Cisco IOS XE 17.18 OSPF](https://www.cisco.com/c/en/us/td/docs/switches/lan/catalyst9200/software/release/17-18/configuration_guide/rtng/b_1718_rtng_9200_cg/configuring_ospf.html)

---

## BGP＝国同士の外交

別組織・ISP・クラウドなどと経路を交換します。

```text
自社ネットワーク
      ↓ BGP
ISP・通信キャリア
      ↓
インターネット
```

「最短だから」だけではなく、**会社の方針**で経路を選べます。

```text
通常通信   → ISP-A
バックアップ → ISP-B
特定拠点宛   → 専用線
```

特徴：

* 🌍 ISP・インターネット・大規模NW向け
* 🧠 ポリシー重視
* 📦 大量の経路を扱える
* 🛡️ 急激な変化より安定性・制御を重視

BGPはAS間の経路交換に使われる外部向けプロトコルです。[Cisco IOS XE 17.18 BGP](https://www.cisco.com/c/en/us/td/docs/switches/lan/catalyst9200/software/release/17-18/configuration_guide/rtng/b_1718_rtng_9200_cg/configuring_bgp.html)

※同一組織内で使う`iBGP`もありますが、入場前は「外部・大規模経路向け」で十分です。

---

# Static・OSPF・BGPの選び方 🚦

| 状況                         | 選択           |
| -------------------------- | ------------ |
| 経路が1～2本で変化しない              | Static Route |
| 社内・拠点内で冗長経路がある             | OSPF         |
| ISP・キャリア・別組織と接続            | BGP          |
| AWS Direct Connect等で動的経路交換 | BGP          |
| 社内はOSPF、インターネット接続はBGP      | 両方           |

```text
社内LAN
  ↓ OSPF
境界Router
  ↓ BGP
ISP・クラウド
```

OSPFとBGPは敵ではなく、**担当範囲が違う仲間**です🤝

---

# どの機器に設定する？ 🔧

```text
アクセスSW
├─ VLAN
├─ NTP
├─ syslog
└─ SNMP

L3・コアSW
├─ 上記すべて
└─ OSPF

境界Router
├─ 上記すべて
├─ OSPF：社内側
└─ BGP：ISP・クラウド側
```

⚠️ 普通のL2アクセススイッチでは、OSPF・BGPを動かしません。
OSPF・BGPを使うのは基本的に**ルータまたはL3スイッチ**です。

## 最短暗記 🧠🔥

> 🚪 VLAN＝分ける
> ⏰ NTP＝時計
> 📝 syslog＝記録
> 📊 SNMP＝監視
> 🗺️ OSPF＝内部経路
> 🌍 BGP＝外部・大規模経路
> 🤖 Ansible＝全部をまとめて設定する司令塔

参照した資料は、現行Ansible公式およびCisco IOS XE 17.18（2025～2026年公開・更新）の一次資料です。
