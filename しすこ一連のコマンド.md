いい。実務でそのまま頭に入る形にする。

今回は **「Cisco SwitchへTera TermでSSH接続 → showで確認 → ログ保存 → 安全に終了」** の一連の流れ。
前提は **調査だけ・設定変更なし**。Catalyst 9300 / IOS XE系を基準にしてる。Cisco IOS XE 26.x公式でも `show running-config`、`show interfaces ...`、`show vlan`、`show ip route`、`show logging` が確認用コマンドとして提供されてる。([Cisco SPG][1])

※下の出力は全部「こう見える」ための架空例。実際の機種・IOS XE・設定で変わる。

---

# 🔥 全体の流れ

```text
① 作業対象を確認
      ↓
② 管理PCからSSH/22疎通確認
      ↓
③ Tera Term起動
      ↓
④ SSH v2で接続
      ↓
⑤ Host Key確認
      ↓
⑥ Login
      ↓
⑦ Tera Termログ開始
      ↓
⑧ hostname / clock / version確認
      ↓
⑨ Interface・VLAN・Trunk確認
      ↓
⑩ MAC / ARP / Route確認
      ↓
⑪ running-config / startup-config確認
      ↓
⑫ show logging確認
      ↓
⑬ 必要な結果を記録
      ↓
⑭ exit
      ↓
⑮ Tera Termログ停止・保存
```

---

# ① 接続前：まず「どの機器を触るか」確認 📋

ここ超大事。

最低限これを確認。

```text
対象機器名      : SW-FLOOR01
管理IP          : 10.100.0.11
接続方法        : SSH
ユーザー        : nwuser
対象Interface   : Gi1/0/10
対象VLAN        : VLAN20
作業内容        : 状態確認のみ
Ticket          : INC-12345
```

🔥 **IPだけ見て接続しない。**

「10.100.0.11だから多分これだろ」は事故の元。

---

# ② Tera Term前にSSHできるか確認

Windowsならまずこれ。

```powershell
Test-NetConnection 10.100.0.11 -Port 22
```

例👇

```text
ComputerName     : 10.100.0.11
RemoteAddress    : 10.100.0.11
RemotePort       : 22
InterfaceAlias   : Ethernet
TcpTestSucceeded : True
```

✅ `True`

→ 少なくとも管理PCから **TCP/22まで到達できてる**。

❌ `False`

→ Tera Term以前に、

```text
Route
FW
管理NW
VPN
踏み台
SSH Service
```

などを疑う。

添付資料でも `Test-NetConnection <host> -Port 443` はTCP Port確認用途として整理されてる。22に変えればSSH確認になる。

---

# ③ Tera Term起動 → SSH接続

Tera Termで、

```text
Host: 10.100.0.11

○ TCP/IP
Service: SSH
TCP port#: 22
SSH version: SSH2
```

として接続。

Tera Term公式でもHost名/IPを指定してTCP/IP接続でき、TTSSHでSSH接続を行える。現在の設定上もSSH2が基本。

---

# ④ 初回接続・鍵変更時：Host Keyを確認 🔐

こんなのが出ることがある。

```text
SECURITY WARNING

The server's host key is not cached...
SHA256:xxxxxxxxxxxxxxxx
```

ここで、

> とりあえずContinue！

はNG。

正しくは、

```text
Tera Term表示Fingerprint
        ↓ 比較
機器台帳・管理者から得たFingerprint
```

一致を確認。

Tera Term公式も、Host Keyが未登録・変更されている場合は警告し、**信頼できるFingerprintと比較する**よう案内してる。

🔥

```text
Host Key
＝「今接続してる相手、本当にそのSwitch？」
```

を確認する身分証。

---

# ⑤ Login

例えば、

```text
User name: nwuser
Password: ********
```

ログイン。

すると、

```text
SW-FLOOR01>
```

みたいになる。

`>` は基本、

**User EXEC mode**

。

---

# ⑥ 必要なら `enable`

確認権限としてPrivileged EXECが必要なら、

```text
SW-FLOOR01> enable
Password:
SW-FLOOR01#
```

となる。

### 覚え方

```text
>
User EXEC
確認できる範囲が狭い

#
Privileged EXEC
show系を広く確認できる

(config)#
設定変更モード 🚨
```

今回の調査では、

```text
SW-FLOOR01#
```

まで。

**`configure terminal` は入らない。**

---

# ⑦ ここでTera Termログ開始 📄

ログイン直後、コマンド打つ前に、

```text
File
 ↓
Log
```

を選択。

ファイル例：

```text
20260913_SW-FLOOR01_INC-12345.log
```

保存場所は当然、

**現場指定フォルダ**。

Tera Term公式でも `[File] → [Log]` でHostから受信したTerminal Dataをファイルへ保存できる。

🔥 ログの目的は、

> 「俺やりました」ではなく
> **「何時に何を確認して、何が表示されたか」**

を残すこと。

---

# ⑧ 最初に「接続先が正しいか」確認

いきなりVLAN見ない。

まずこれ👇

## `show clock`

```text
SW-FLOOR01# show clock
23:10:32.145 JST Sun Sep 13 2026
```

見るところ：

```text
時刻合ってる？
Timezone合ってる？
```

ログと障害発生時刻を比較するために重要。

Cisco公式でも `show clock` はDeviceの日時確認用。([Cisco][2])

---

## `show version`

```text
SW-FLOOR01# show version

Cisco IOS XE Software, Version 17.xx
Cisco Catalyst 9300
...
SW-FLOOR01 uptime is 42 weeks, 3 days
```

見るところ👇

```text
機種
IOS XE Version
Uptime
再起動されてないか
```

🔥

> 「機器間違えてない？」
> 「直前に再起動してない？」

を見る。

Cisco公式でも `show version` はHardware、Software version、Boot image等の確認用。([Cisco][3])

---

# ⑨ Port状態をざっくり確認

## `show interfaces status`

```text
SW-FLOOR01# show interfaces status

Port      Name          Status       Vlan  Duplex Speed
Gi1/0/1   Uplink-SW02   connected    trunk full   1000
Gi1/0/10  Printer-A     connected    20    full   1000
Gi1/0/11  PC-001        notconnect   10    auto   auto
Gi1/0/12  Spare         disabled     1     auto   auto
```

見る👇

```text
Gi1/0/10
Status = connected
VLAN   = 20
```

🔥 まず、

> **「刺さってる？」「どのVLAN？」**

を確認。

Cisco公式でもInterfaceのStatus確認用コマンド。([Cisco][3])

---

# ⑩ 対象Portを詳しく見る

## `show interfaces Gi1/0/10`

```text
SW-FLOOR01# show interfaces Gi1/0/10

GigabitEthernet1/0/10 is up, line protocol is up
  Hardware is Gigabit Ethernet
  MTU 1500 bytes
  Full-duplex, 1000Mb/s
  5 minute input rate 12000 bits/sec
  5 minute output rate 9000 bits/sec
  0 input errors
  0 CRC
  0 frame
  0 output errors
```

見るところ👇

```text
up / down
Speed
Duplex
CRC
Input errors
Output errors
Drop等
```

例えば、

```text
123 input errors
98 CRC
```

が継続増加なら、

```text
Cable
SFP
NIC
物理Port
```

などL1を疑う。

---

# ⑪ VLAN確認

## `show vlan brief`

```text
SW-FLOOR01# show vlan brief

VLAN Name              Status    Ports
---- ----------------- --------- ---------------------
1    default           active
10   USER              active    Gi1/0/11, Gi1/0/12
20   PRINTER           active    Gi1/0/10
30   SERVER            active
99   MANAGEMENT        active
```

見る👇

```text
VLAN20は存在する？
Gi1/0/10はVLAN20？
```

Cisco IOS XE 26.x公式でも `show vlan brief` はVLAN状態確認用。([Cisco][4])

---

# ⑫ InterfaceのSwitchport設定を見る

## `show interfaces Gi1/0/10 switchport`

```text
SW-FLOOR01# show interfaces Gi1/0/10 switchport

Name: Gi1/0/10
Switchport: Enabled
Administrative Mode: static access
Operational Mode: static access
Access Mode VLAN: 20 (PRINTER)
Trunking Native Mode VLAN: 1 (default)
```

🔥 これはかなり使える。

見るのは、

```text
Access？
Trunk？
Access VLANいくつ？
```

Cisco公式でもSwitch PortのAdministrative / Operational状態確認に使う。([Cisco][3])

---

# ⑬ Trunk確認

上位SWとのPortなら、

## `show interfaces trunk`

```text
SW-FLOOR01# show interfaces trunk

Port        Mode   Encapsulation Status    Native vlan
Gi1/0/1     on     802.1q        trunking  1

Port        Vlans allowed on trunk
Gi1/0/1     10,20,30,99
```

例えばVLAN30が必要なのに、

```text
Allowed VLAN
10,20,99
```

なら、

🚨 VLAN30は上位へ行けない。

覚え方：

```text
show vlan brief
→ このSwitchにVLANある？

show interfaces trunk
→ 上へそのVLAN運べる？
```

---

# ⑭ MAC Address Table

例えばPrinterのMACが、

```text
AAAA.BBBB.CCCC
```

なら、

## `show mac address-table address aaaa.bbbb.cccc`

例👇

```text
SW-FLOOR01# show mac address-table address aaaa.bbbb.cccc

Vlan    Mac Address       Type       Ports
----    -----------       --------   -----
20      aaaa.bbbb.cccc    DYNAMIC    Gi1/0/10
```

これで、

> **MAC `aaaa.bbbb.cccc` はVLAN20のGi1/0/10の先にいる**

と分かる。

Cisco公式にもMAC Address TableをInterface/VLAN/MAC単位で表示するコマンドがある。([Cisco][2])

🔥

```text
MAC Table
MAC + VLAN → Switch Port
```

---

# ⑮ ARP確認

L3SWなら、

## `show ip arp`

```text
SW-CORE01# show ip arp

Protocol Address        Age  Hardware Addr   Type Interface
Internet 192.168.20.50  2    aaaa.bbbb.cccc ARPA Vlan20
```

これなら、

```text
192.168.20.50
       ↕
aaaa.bbbb.cccc
```

の対応。

🔥

```text
ARP
IP → MAC

MAC Table
MAC → Port
```

だから、

```text
IP
 ↓ ARP
MAC
 ↓ MAC Table
Switch Port
```

と追える。

Cisco公式でも `show ip arp` はIP ARP cache確認用。([Cisco][5])

---

# ⑯ L3 Interface / SVI確認

## `show ip interface brief`

```text
SW-CORE01# show ip interface brief

Interface    IP-Address       OK? Method Status Protocol
Vlan10       192.168.10.1     YES manual up     up
Vlan20       192.168.20.1     YES manual up     up
Vlan30       192.168.30.1     YES manual up     up
Gi1/0/48     unassigned       YES unset  up     up
```

例えば、

```text
Vlan20
192.168.20.1
up/up
```

なら、

**VLAN20のSVI/Gateway候補が生きてる**。

前にやった👇

> SVI＝VLANのL3出口

がここで出てくる。

---

# ⑰ Route確認 🛣️

## `show ip route 192.168.50.10`

例👇

```text
SW-CORE01# show ip route 192.168.50.10

Routing entry for 192.168.50.0/24
  Known via "ospf 10"
  Routing Descriptor Blocks:
  * 10.0.0.2
      Route metric is 20
```

見る👇

```text
Destination
Next Hop
どのRouting Protocol？
```

つまり、

> 「192.168.50.10への道知ってる？」

を見るコマンド。

Cisco公式でも `show ip route` は現在のRouting Table確認用。([Cisco][5])

---

# ⑱ 現在Config確認

ここからConfig。

## `show running-config`

```text
SW-FLOOR01# show running-config

Building configuration...

Current configuration : 15432 bytes
!
hostname SW-FLOOR01
!
vlan 10
 name USER
!
vlan 20
 name PRINTER
!
interface GigabitEthernet1/0/10
 description Printer-A
 switchport access vlan 20
 switchport mode access
!
...
```

意味👇

> **今、実際に動いてる設定**

Cisco公式でも `show running-config` はCurrent Running Configurationを表示する。([Cisco SPG][1])

ただし🔥

**Config全部を出す必要がないなら絞る方がいい。**

理由：

```text
AAA
SNMP
TACACS
証明書
認証関連
```

など機密情報を含む可能性がある。

---

# ⑲ 対象InterfaceのConfigだけ見る

こっち超おすすめ。

## `show running-config interface Gi1/0/10`

```text
SW-FLOOR01# show running-config interface Gi1/0/10

Building configuration...

interface GigabitEthernet1/0/10
 description Printer-A
 switchport access vlan 20
 switchport mode access
 spanning-tree portfast
end
```

これだけで、

```text
Description
Access VLAN
Access/Trunk
STP関連
```

などが見える。

🔥 実務だと、

> **全部のConfigを見るより対象箇所だけ見る**

方が速い。

Cisco公式も、特定Interface確認には `show running-config interface` が有用としてる。([Cisco SPG][1])

---

# ⑳ Startup Config

## `show startup-config`

例👇

```text
SW-FLOOR01# show startup-config

Using 15201 out of 2097152 bytes
!
hostname SW-FLOOR01
!
interface GigabitEthernet1/0/10
 description Printer-A
 switchport access vlan 20
 switchport mode access
...
```

意味：

```text
running-config
＝今動いてる設定 🏃

startup-config
＝再起動時に読む保存設定 💾
```

例えば、

```text
Running  VLAN30
Startup  VLAN20
```

なら、

> 「設定変更したけど保存されてない？」

という可能性が出る。

⚠️ ただしここで勝手に

```text
copy running-config startup-config
```

しない。

これは**保存＝変更行為**だから、手順・承認がある時だけ。

Cisco公式でも `copy running-config startup-config` は設定保存操作。([Cisco][3])

---

# ㉑ Log確認

## `show logging`

例えば👇

```text
SW-FLOOR01# show logging

Syslog logging: enabled

Log Buffer:
Sep 13 22:41:12: %LINK-3-UPDOWN:
 Interface GigabitEthernet1/0/10, changed state to down

Sep 13 22:41:15: %LINEPROTO-5-UPDOWN:
 Line protocol on Interface GigabitEthernet1/0/10,
 changed state to down

Sep 13 22:42:03: %LINK-3-UPDOWN:
 Interface GigabitEthernet1/0/10, changed state to up
```

これを見ると、

```text
22:41 Down
 ↓
約50秒後
 ↓
Up
```

となってる。

つまり、

> **Link Flapした？**

という証拠になる。

Cisco公式でも `show logging` はSystem Logging状態とLog Buffer内容を表示するコマンド。([Cisco SPG][6])

🔥 ログは、

```text
障害発生時刻
 ↓
同時刻のCisco Log
```

を合わせる。

---

# ㉒ STPも必要なら確認

## `show spanning-tree`

例えば、

```text
VLAN0020
  Root ID    Priority 24596
             Address 1111.2222.3333

Interface  Role Sts Cost
Gi1/0/1    Root FWD 4
Gi1/0/2    Altn BLK 4
```

ここで、

```text
Gi1/0/2
Altn BLK
```

でも即障害じゃない。

前にやった通り、

> **STPがLoop防止のため正常にBlockしてる可能性**

がある。

---

# ㉓ 必要最低限コマンドまとめ

これだけ最初は覚えればかなり戦える👇

```text
show clock
→ 時刻

show version
→ 機種・IOS・Uptime

show interfaces status
→ Port一覧

show interfaces Gi1/0/10
→ Port詳細・CRC等

show vlan brief
→ VLAN

show interfaces Gi1/0/10 switchport
→ Access/Trunk・VLAN

show interfaces trunk
→ Trunk・Allowed VLAN

show mac address-table
→ MAC → Port

show ip arp
→ IP → MAC

show ip interface brief
→ SVI/IP/up-down

show ip route
→ Route

show running-config
→ 現在Config

show running-config interface Gi1/0/10
→ 対象Port Config

show startup-config
→ 保存Config

show logging
→ Log

show spanning-tree
→ STP
```

添付チートシートの最低限Cisco確認コマンドもほぼこの構成。

---

# ㉔ 確認完了したら「最後の状態」を残す

例えば最後に、

```text
SW-FLOOR01# show clock
23:25:42.021 JST Sun Sep 13 2026
```

を取る。

これでログ上、

```text
いつ開始
何確認
いつ終了
```

が追いやすい。

---

# ㉕ SSH Session終了

設定モードに入ってないことを確認して、

```text
SW-FLOOR01# exit
```

例：

```text
Connection to 10.100.0.11 closed.
```

Tera Termが切断。

🔥 窓の「×」で強制終了でもSessionは切れるけど、基本は `exit` で正常終了の方が分かりやすい。

---

# ㉖ Tera Term Log終了

Tera Term Log画面で、

```text
Close
```

してログ終了。

Tera Term公式でもLog DialogのCloseでLoggingを終了できる。

その後ファイルを確認。

```text
20260913_SW-FLOOR01_INC-12345.log
```

最低限見る👇

```text
対象機器名ある？
show clockある？
実行Command残ってる？
必要なOutput残ってる？
終了時刻分かる？
```

---

# 🎯 実務での「最短テンプレ」

現場入って固まったら、これを思い出せばいい。

```text
① 接続先確認
     ↓
② Test-NetConnection IP -Port 22
     ↓
③ Tera Term SSH2
     ↓
④ Host Key確認
     ↓
⑤ Login / enable
     ↓
⑥ Tera Term Log開始
     ↓
⑦ show clock
⑧ show version
     ↓
⑨ show interfaces status
⑩ show interfaces <IF>
     ↓
⑪ show vlan brief
⑫ show interfaces <IF> switchport
⑬ show interfaces trunk
     ↓
⑭ show mac address-table
⑮ show ip arp
⑯ show ip interface brief
⑰ show ip route
     ↓
⑱ show run interface <IF>
⑲ 必要ならshow running-config
⑳ 必要ならshow startup-config
     ↓
㉑ show logging
㉒ 必要ならshow spanning-tree
     ↓
㉓ show clock
     ↓
㉔ exit
     ↓
㉕ Tera Term Log終了・保管
```

そして一番大事なのはこれ👇

```text
show
↓
証拠を取る 👀

configure terminal
↓
設定を変える 🔥
```

**調査作業なら基本 `#` のshow系で止める。** `shutdown` / `no shutdown`、`clear`、`reload`、`copy running-config startup-config` などは「確認」ではない。現場手順・承認なしでは触らない。添付資料でもこの境界が明確にされてる。

[1]: https://spg.xgslb-v3.cisco.com/c/en/us/td/docs/switches/lan/catalyst9300/software/release/26-x/command_reference/b_26x_9300_cr/system_management_commands.html?utm_source=chatgpt.com "Command Reference, Cisco IOS XE 26.x.x (Catalyst 9300 Switches) - System Management Commands [Cisco Catalyst 9300 Series Switches] - Cisco"
[2]: https://www.cisco.com/c/en/us/td/docs/switches/lan/catalyst9300/software/release/26-x/configuration_guide/sys_mgmt/b_26x_sys_mgmt_9300_cg/administering_the_device.html?utm_source=chatgpt.com "System Management Configuration Guide, Cisco IOS XE 26.x.x (Catalyst 9300 Switches) - Administering the Device [Cisco Catalyst 9300 Series Switches] - Cisco"
[3]: https://www.cisco.com/c/en/us/td/docs/switches/lan/catalyst9300/software/release/26-x/configuration_guide/int_hw/b_26x_int_and_hw_9300_cg/configuring_interface_characteristics.html?utm_source=chatgpt.com "Interface and Hardware Components Configuration Guide, Cisco IOS XE 26.x.x (Catalyst 9300 Switches) - Configuring Interface Characteristics [Cisco Catalyst 9300 Series Switches] - Cisco"
[4]: https://www.cisco.com/c/en/us/td/docs/switches/lan/catalyst9300/software/release/26-x/configuration_guide/vlan/b_26x_vlan_9300_cg/configuring_vlans.html?utm_source=chatgpt.com "VLAN Configuration Guide, Cisco IOS XE 26.x.x (Catalyst 9300 Switches) - Configuring VLANs [Cisco Catalyst 9300 Series Switches] - Cisco"
[5]: https://www.cisco.com/c/en/us/td/docs/switches/lan/catalyst9300/software/release/26-x/configuration_guide/rtng/b_26x_rtng_9300_cg/configuring_ip_unicast_routing.html?utm_source=chatgpt.com "IP Routing Configuration Guide, Cisco IOS XE 26.x.x (Catalyst 9300 Switches) - Configuring IP Unicast Routing [Cisco Catalyst 9300 Series Switches] - Cisco"
[6]: https://spg.xgslb-v3.cisco.com/c/en/us/td/docs/switches/lan/catalyst9300/software/release/26-x/command_reference/b_26x_9300_cr/tracing.html?utm_source=chatgpt.com "Command Reference, Cisco IOS XE 26.x.x (Catalyst 9300 Switches) - Tracing Commands [Cisco Catalyst 9300 Series Switches] - Cisco"
